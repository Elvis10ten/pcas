package com.fluentbuild.pcas.ledger.models

/*import kotlin.test.*

class PropertyEntityTest {

    @Test
    fun `equals WHEN entity's serviceId & bondId are equal SHOULD return true`() {
        val entity1 = PropertyEntity(1, 2, true, 1.0)
        val entity2 = PropertyEntity(1, 2, false, 2.0)
        assertEquals(entity1, entity2)
    }

    @Test
    fun `equals WHEN both entity's serviceId & bondId are not equal SHOULD return false`() {
        val hasUsage = false
        val rank = 1.0

        val entity1 = PropertyEntity(1, 2, hasUsage, rank)
        val entity2 = PropertyEntity(1, 3, hasUsage, rank)
        val entity3 = PropertyEntity(2, 2, hasUsage, rank)
        val entity4 = PropertyEntity(4, 5, hasUsage, rank)

        assertNotEquals(entity1, entity2)
        assertNotEquals(entity1, entity3)
        assertNotEquals(entity1, entity4)
    }
}*/