package com.fluentbuild.pcas.ledger.utils

/*import kotlin.test.assertEquals
import kotlin.test.assertTrue

internal fun <T> assertContentEquals(actual: Collection<T>, expected: Collection<T>) {
    assertEquals(actual.size, expected.size)
    actual.forEach { assertTrue(expected.contains(it), "Item: $it\nNot found in expected: $expected") }
    expected.forEach { assertTrue(actual.contains(it), "Item: $it\nNot found in actual: $actual") }
}*/