package com.fluentbuild.pcas.ledger.models

/*import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertNotEquals

class BondEntityTest {

    @Test
    fun `equals WHEN entity's serviceId & bondId are equal SHOULD return true`() {
        val entity1 = BondEntity(1, 2)
        val entity2 = BondEntity(1, 2)
        assertEquals(entity1, entity2)
    }

    @Test
    fun `equals WHEN entity's serviceId & bondId are not equal SHOULD return false`() {
        val entity1 = BondEntity(1, 2)
        val entity2 = BondEntity(1, 3)
        val entity3 = BondEntity(2, 2)
        val entity4 = BondEntity(4, 5)
        assertNotEquals(entity1, entity2)
        assertNotEquals(entity1, entity3)
        assertNotEquals(entity1, entity4)
    }
}*/